info:
  name: Filtro búsqueda nombre parcial
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/buscarNombre?q=zelda
  params:
    - name: q
      value: zelda
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
