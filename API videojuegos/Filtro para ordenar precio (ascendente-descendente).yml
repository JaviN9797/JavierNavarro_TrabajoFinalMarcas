info:
  name: Filtro para ordenar precio (ascendente/descendente)
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/ordenarPrecio?order=asc
  params:
    - name: order
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
