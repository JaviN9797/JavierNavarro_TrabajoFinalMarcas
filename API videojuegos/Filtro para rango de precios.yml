info:
  name: Filtro para rango de precios
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/buscarPrecio?min=0&max=20
  params:
    - name: min
      value: "0"
      type: query
    - name: max
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
