info:
  name: Para agrupar por un campo específico
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/videojuegos/agrupar?campo=empresa
  params:
    - name: campo
      value: empresa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
